/**
 * 画面パス
 */
export const PAGE_PATHS = Object.freeze({
  HOME: "/",
  LOGIN: "/login",
  JOIN: "/join",
});

/**
 * APIパス
 */
export const API_PATHS = Object.freeze({
  USERS: "/users",
  USERS_ME: "/users/me",
  LOGIN: "/login",
  LOGOUT: "/logout",
});
